import { Text, View } from "react-native";
import styled from "styled-components/native";

export const ViewCategoryStyled = styled(View)`
	flex-direction: row;
	padding: 3px;
	border-color: gray;
	border-width: 2px;
	border-radius: 10px;
	gap: 4px;
`;
