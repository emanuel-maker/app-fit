import { SafeAreaView, Text, View } from "react-native";
export default function Profile() {
	return (
		<SafeAreaView style={{ backgroundColor: "#292929", height: "100%" }}>
			<Text style={{ paddingLeft: 4, fontSize: 24, fontWeight: "bold" }}>
				Config Screen
			</Text>
		</SafeAreaView>
	);
}
